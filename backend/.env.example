# Example environment variables for Prisma
# Copy this file to `.env` and fill in your real credentials.

# Primary application database URL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/your_app_db?schema=public"

# Shadow database used by Prisma Migrate for schema diffing.
# Create a dedicated empty database for migrations to avoid locks and slow queries.
# Example SQL (run in psql or a DB admin tool):
#   CREATE DATABASE prisma_migrate_shadow_db;

# Then use a connection URL pointing to that shadow DB:
# Format: postgresql://USER:PASSWORD@HOST:PORT/prisma_migrate_shadow_db?schema=public
SHADOW_DATABASE_URL="postgresql://user:password@localhost:5432/prisma_migrate_shadow_db?schema=public"

# NOTE: Do NOT point the shadow DB to the same database as `DATABASE_URL`.
